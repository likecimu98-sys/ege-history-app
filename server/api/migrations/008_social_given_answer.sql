-- Что именно ответил ученик.
--
-- Учитель видел в разборе только «✅/❌» и баллы: понять, В ЧЁМ ошибка, было
-- нельзя. Без этого разбор домашней работы бессмысленен — нечего объяснять
-- классу, кроме факта «не сдал».
--
-- 🔴 Колонка добавляется с пустым значением по умолчанию, а не заполняется
-- задним числом: ответы прошлых попыток нигде не сохранялись и восстановить их
-- неоткуда. Кабинет обязан показывать такие строки честно — «ответ не
-- сохранён», а не выдавать пустоту за ответ.
ALTER TABLE social_attempt_events
  ADD COLUMN IF NOT EXISTS given_answer text NOT NULL DEFAULT '';
