# AGENTS.md — общий договор для ИИ-агентов (Claude Code, ChatGPT/Codex и др.)

Над этим проектом работают несколько ИИ-ассистентов **по очереди**. Этот файл — единый
источник правды, чтобы мы не переделывали и не «обходили костылями» работу друг друга.

## Правило №1 (обязательное для КАЖДОГО агента)
1. **В начале сессии — прочитай этот файл целиком** (и последние записи в «Журнале передачи»).
2. **В конце значимой работы — обнови его**: изменил архитектуру/деплой/инварианты →
   поправь соответствующий раздел; добавь строку в «Журнал передачи».
3. **Нашёл расхождение с тем, что тут написано, — не обходи молча.** Сначала выясни, кто и
   зачем так сделал (спроси пользователя / посмотри git log), потом чини корень и обнови файл.
4. **Не чини симптом костылём.** Если правишь чужое решение — объясни в коммите почему и
   отрази в этом файле, чтобы следующий агент не откатил вслепую.

---

## Что это за проект
PWA-тренажёр для подготовки к ЕГЭ по истории (ваниль JS, без сборщика для JS; CSS — Tailwind
в `output.css`). Прод: **https://reshay-istoriyu.ru**. Клиент — Telegram Mini App
(@Reshay_istoriyu_bot), большинство пользователей **на мобильном**.

## Архитектура (после миграции Firebase→PostgreSQL, июль 2026)
- **CDN: сайт стоит ЗА Cloudflare (с 2026-07-22).** Домен `reshay-istoriyu.ru` (регистратор
  REG.RU) делегирован на NS `demi.ns.cloudflare.com` / `melnicoff.ns.cloudflare.com`; A-записи
  (root+www, Proxied) ведут на origin `185.198.152.200`. Причина: «не открывается на части
  устройств / ERR_TIMED_OUT» после миграции оказалось нестабильной сетевой достижимостью
  ОДНОГО голого IP (запросы не доходили до nginx вовсе); CDN с anycast-IP это вылечил —
  проверено на реальных застрявших устройствах. НЕ отключай проксирование (серые облачка)
  без явного решения пользователя. Настройки CF: SSL **Full**, WebSockets On, Browser Cache
  TTL = Respect Existing Headers (наши `no-cache` уважаются, `cf-cache-status: DYNAMIC/MISS`).
  Следствия: в nginx access.log клиентские IP теперь Cloudflare (162.158.x/172.70.x и др.) —
  РЕАЛЬНЫЙ IP бери из первой записи `X-Forwarded-For` (API так и делает: `trustProxy=true`,
  рейт-лимит считает по реальным IP — не «чини» это). TODO по желанию: nginx real_ip
  (`CF-Connecting-IP`) для красивых логов; фаервол «80/443 только с IP Cloudflare» — ТОЛЬКО
  вместе с Cloudflare Origin Certificate (иначе сломается продление Let's Encrypt).
- **Хостинг (origin):** VPS `185.198.152.200` (root), Nginx `root /var/www/ege-app` (обычная папка),
  Let's Encrypt. `location /auth/` проксируется в API. Конфиг сайта:
  `/etc/nginx/sites-enabled/reshay-istoriyu.ru`.
- **Данные/синк:** PostgreSQL на VPS (миграция с Firebase завершена). Клиентские модули синка —
  `cloud-sync.js` + `vps-sync-compat.js`. Старый `firebase-sync.js` выведен из клиента и
  **не должен попадать в прод-архив** (`.gitattributes export-ignore`).
- **Бот:** `/root/bot/` на VPS (pm2). API/инфра: `/opt/ege-history-api`, `/opt/ege-history-infra`.
- **Кэш клиента:** Service Worker с версией `APP_VERSION` в `service-worker.js`
  (формат сейчас `2026-07-DD-vps-N`). **Меняешь кэшируемый JS/HTML/CSS — обязательно подними
  версию SW и общий `?v=RELEASE` всех стартовых JS/CSS**, иначе старый worker может собрать
  новый HTML со старыми файлами. Запись ответа в CacheStorage не должна блокировать выдачу
  сетевого ответа странице. В Telegram на iOS Service Worker намеренно отключён из-за
  зависаний WKWebView CacheStorage; обычная PWA в Safari остаётся офлайн.

## ДЕПЛОЙ — читай внимательно, тут была главная путаница
- **Рутинный деплой сайта → `powershell -File deploy-static.ps1`.**
  git archive HEAD → scp → распаковка в `/var/www/ege-app` с атомарной заменой и снимком
  отката `ege-app.prev-*`. **Базу данных не трогает.** Это правильный скрипт для обычных правок.
- **Деплой серверного API → `powershell -File deploy-api.ps1`** (создан 26.07.2026).
  `git archive HEAD:server/api` → scp → `npm ci --omit=dev` → `node --test` НА СЕРВЕРЕ →
  атомарная замена `/opt/ege-history-api` → `pm2 restart hist-api` → health-check
  `127.0.0.1:8792/api/v1/health` 20 секунд → **автоматический откат, если не ответил**.
  Снимки `ege-history-api.prev-*` (хранятся три). БД, миграции и `/etc/ege-history/api.env`
  не трогает — миграции гоняются отдельно (`npm run migrate` в server/api).
  ⚠️ **Правку API руками на боевом сервере не делай.** Именно так `src/auth.js` разошёлся
  с репозиторием на сутки и остался бы миной под любым будущим деплоем. Нужна срочная
  правка — делай в репозитории и гони скриптом; он быстрее ручного scp и умеет откат.
  ⚠️ Удалённая часть едет **файлом** (scp + `bash`), а не аргументом ssh: PowerShell 5.1
  корёжит вложенные кавычки, а в скрипте есть sed/curl со своими кавычками.
- **`deploy-vps.ps1` — это РАЗОВЫЙ cutover Firebase→PostgreSQL, НЕ рутинный деплой.**
  С `-Cutover -ConfirmCutover RESHAY_HISTORY_VPS` он **переключает боевую БД**, импортирует
  Firestore, ставит окна отката. **Не запускай его для правок сайта.** Cutover уже выполнен.
  Его снимки отката `ege-app.rollback-*` / `*.client-rollback-*` — 60-дневная страховка,
  их удалять/трогать нельзя.
- **SSH:** строгая проверка host key (`StrictHostKeyChecking=yes`, только `ssh-ed25519`,
  ключ `~/.ssh/id_ed25519`). Отпечаток сервера:
  `SHA256:2QvI08yVRpFJ4D35RElDDHmPBoMl88u9+qT0AhNcVKs`. **Не отключай строгую проверку и не
  делай `ssh-keyscan`** — это обесценит пиннинг.
- **Профиль Windows с пробелом** («Хозяин Саша») ломает путь `UserKnownHostsFile` в ssh.
  Оба .ps1 используют **DOS 8.3-короткие пути** (`C:\Users\90DF~1\...`). PowerShell 5.1 корёжит
  встроенные кавычки при передаче в native ssh/scp — поэтому именно короткие пути, не кавычки.
- **.ps1 — только ASCII** (PowerShell 5.1 читает скрипты в ANSI без BOM; кириллица → парс-ошибки).
- `deploy-static.ps1` после распаковки нормализует mtime файлов по часам VPS: локальные часы
  могут быть впереди сервера, а будущий `Last-Modified` ломает браузерную ревалидацию кэша.
- После деплоя: `git push origin master` (GitHub — запасной адрес/бэкап).

## Инварианты — НЕ ломать без явного согласия пользователя
- Безопасность деплоя: строгий host key + ed25519-пиннинг (см. выше).
- Прод-архив НЕ содержит `server/`, `firebase-sync.js` (гарды в обоих .ps1 — сохраняй их).
- Firestore/доступы, дневные лимиты строк, ролевые правила учителей — менять только по задаче.
- Тема: в приложении оставлена ТОЛЬКО `skin-classic` (пикер тем удалён). Другие скины в
  `theme-aurora.css` — мёртвый груз, но `body.skin-classic` активна всегда; правя чипы/цвета,
  проверяй И светлую, И тёмную тему (`html.dark`).
- **iOS/кэш — два инварианта против «белого экрана на айфонах» (стоили нам недели):**
  1. **Nginx отдаёт `index.html` и весь JS/CSS с `Cache-Control: no-cache`** (`location /`).
     Без него iOS WKWebView кэширует вход эвристически (только `Last-Modified`) и НЕ ходит за
     обновлениями — новые деплои не доезжают до айфонов. Долгий кэш — только у картинок/медиа/
     шрифтов (отдельный `location ~*`). SW и манифест — тоже `no-cache`. НЕ убирай эти заголовки.
  2. **Service Worker НИКОГДА не завязывает ответ страницы на CacheStorage.** На iOS
     `cache.match`/`cache.put` под нагрузкой (20 defer-скриптов) сериализуются и могут не
     ответить → `respondWith` висит → белый экран навсегда. Правило: чтение кэша — под таймаутом
     (`CACHE_READ_TIMEOUT_MS`), запись — только фоновая (`waitUntil`/fire-and-forget), install не
     блокирует активацию на кэшировании. НЕ возвращай `await cache.put(...)` на путь ответа.

## Рабочее соглашение
- **Проверяй в браузере перед деплоем** (превью на localhost). Для UI — на мобильном 375px
  (целевая среда). Не выкладывай непроверенное на прод.
- Коммиты — осмысленные, с префиксом (`fix/feat/chore(область)`), на русском ок.
- Ветка по умолчанию — `master`. Прод = локальный master (может быть впереди origin, т.к.
  часть работы не в GitHub). Не делай force-push и деструктивных reset без согласия.
- Крупные/необратимые действия (переключение БД, миграции, удаление данных, смена настроек
  сервера) — только с явного «да» пользователя.

---

## Журнал передачи (новые записи — сверху)
- **2026-07-26 (Claude, вечер): пробник, перетаскивание, разбор ошибок.**
  ⚠️ **Главное — баг оценивания, живший в проде.** В `exam-scoring.js` функция
  `textWarnings()` обе проверки формы записи делала ПО ВВОДУ УЧЕНИКА, не сверяясь
  с эталоном: `case` срабатывал на любой строчной букве (то есть на любом русском
  слове), `spacing` — на любом неалфавитном символе (то есть на «Ярослав Мудрый»,
  где пробел есть и в эталоне). Флаг `acceptedWithWarning` красит ответ как
  неверный и кладёт задание в разбор ошибок — значит **все 215 текстовых заданий
  банка (8–11) считались ошибочными при верном ответе**, а счётчик ошибок у всех
  решавших раздут. Правило на будущее: проверка формы ответа обязана СРАВНИВАТЬ,
  а не тестировать одну строку. `case` убран совсем — регистр на ЕГЭ не критерий.
  ⚠️ **Осталось:** старые ложные записи в `stats.mockExamMistakes` сами не уйдут,
  нужна разовая чистка записей с полным баллом (решение за пользователем).
  **Ротация пробника.** `stats.examSolved` — id верно решённых и `g:<groupId>` для
  карт 9–12. Из пула уходит только верное; ошибочное остаётся. Круг по номеру
  закончился — отметки ПО ЭТОМУ номеру сбрасываются. Поле добавлено в `SAVE_FIELDS`
  (state.js) и `CLOUD_STATE_FIELDS` (cloud-sync.js), при слиянии копий —
  ОБЪЕДИНЕНИЕ, не max. Замер: у задания 8 в банке всего 23 варианта и 7 из них про
  марки — ротация это смягчает, но банк надо допарсить.
  **Перетаскивание** (table.js, `initChipDrag`): Pointer Events, не HTML5 DnD.
  Заканчивается вызовом `handleSlotClick()` — своей логики ответа НЕТ и заводить
  нельзя. `.dnd-chip` получил `touch-action: none`, иначе жест уходит прокрутке.
  ⚠️ Гашение клика после переноса — одноразовая ловушка на 400 мс, НЕ флаг: после
  переноса пальцем браузер клик не синтезирует, и флаг съедал следующий тап.
  **Подсветка:** пустые слоты больше не подсвечиваются (`.slot-ready` без правил),
  подсвечивается одна цель под пальцем (`.drop-target`).
- **2026-07-26 (Claude): выбор ответа, статистика и кабинет учителя.**
  **1) «Выделяется чем-то мутным».** Причина не в свечении, а в наследстве авроры:
  `body.skin-classic .dnd-chip { background:#fff!important; border-color:#cbd5e1!important }`
  в theme-classic.css перебивало `.dnd-chip.selected` (у состояния важности нет).
  От выделения выживал только полупрозрачный ореол `0 0 0 3px rgba(59,130,246,.3)`.
  В тёмной теме `.selected` был прописан отдельно с !important и работал — поэтому
  баг видели только на светлой. Такая же строка глушила `.dnd-slot.has-item`:
  слот с ответом выглядел ровно как пустой. Строки удалены.
  ⚠️ **Правило:** в theme-classic.css не должно быть `!important` на базовых
  элементах — он бьёт по состояниям (`:hover`, `.selected`, `.has-item`), у которых
  важности нет и не будет. Если правило нужно — вешай его на состояние тоже.
  Заодно ореолы `0 0 0 3px rgba(...)` убраны из ВСЕХ состояний слота: на светлом
  фоне полупрозрачное свечение читается как грязь и вылезает за границу ячейки,
  где `td { overflow: hidden }` срезает его снизу — отсюда «рамка не закрыта».
  Цвет теперь несут рамка, заливка и текст (внутри border-box), объём — `--e-2`.
  **2) Статистика.** Было 4 цвета цифр, 4 подписи капсом в разрядку по 10px и
  пять зелёных «0%». Стало: подписи обычным регистром 12px, цифры одного размера
  и цвета текста + `tabular-nums`, цветной остался один показатель (ошибки),
  ноль гаснет через `data-zero`. Компоненты `.stat-card/.stat-row/.stat-h3`.
  **3) Кабинет учителя.** Было: 11 подписей кеглем 8px в одной карточке, 5 цифр
  KPI пятью цветами, 5 кнопок пятью пастельными фонами, синий градиент во всю
  ширину панели (плейсхолдеры «код»/«год» — белым по white/15, контраст <2:1),
  разделители захардкожены как `#f1f5f9` БЕЗ тёмного варианта. Стало: компоненты
  `.tc-*` на токенах, цветом кодируются только точность и статус-бейджи.
  Также: `renderEraRows(null)` роняло рендер ВСЕГО списка учеников — добавлена
  защита; сегменты переехали с девяти Tailwind-классов на один `aria-pressed`.
  Замер: кеглей меньше 11px по проекту 280 → 210.

- **2026-07-26 (Claude): на ПК задание открывалось ПОД лобби. Теперь это диалог.**
  Правило `#lobby-area { display: grid }` из ПК-блока (специфичность 1,0,0) молча
  перебивало tailwind-овский `.hidden` (0,1,0): JS честно вешал класс, лобби честно
  оставалось на экране, задание пристраивалось следом — за нижним краем окна. На
  телефоне бага не было, там лобби разложено флекс-утилитами. **Правило: не задавай
  `display` по ID, если этот же элемент прячут классом.** В styles.css уже был
  комментарий про ту же ловушку у `.card-row.hidden` — и я всё равно наступил, потому
  что ID проигрывает тише.
  Одной специфичности мало: на ПК подмена куска в середине большой страницы не
  читается как «экран сменился». Теперь с 1100px задание — **диалог**: лобби остаётся
  видимым фоном под затемнением (`--c-scrim`, новый токен: в разметке было три разных
  `bg-black/60|70|80` без причины), диалог `min(1040px, 100vw-72px)`, центр экрана,
  `max-height: 100vh - 40px` со своей прокруткой, рамка + `--e-3`. Ширина больше меры
  строки намеренно: внутри не проза, а таблица + пул в две колонки.
  `pointer-events: none` на фоне снимает мышь, но НЕ убирает кнопки из обхода по Tab —
  за это отвечает `inert` (`initLobbyInert()` в ui.js). Сделан наблюдателем за классом
  `in-game`, а не правкой по месту: класс вешают пять разных мест (app.js ×3, modes.js,
  visual-trainer.js), пятая правка потерялась бы.
  Проверено на 1440×900 / 1280×620 / 1280×500 и 375: task1/4/5/7, карточки, сюжеты,
  карандаш, архив, визуал — влезают или прокручиваются внутри диалога; мобайл не задет
  (лобби `display:none`, игра `relative`, затемнения нет). Свои оверлеи (свайп, подбор,
  ВОВ, зубрёжка, пробник) начинаются с z-10050 и выше затемнения (60) — не задеты.
- **2026-07-25 (Claude): ПК — отдельный класс устройства.** Было 235 брейкпоинтов
  `sm:`, 13 `lg:` и НИ ОДНОГО `xl:`; контент упирался в `max-w-5xl`, на 1280px по бокам
  простаивало 288px. Лобби разбито на четыре блока — `#lobby-main` / `#lobby-nav` /
  `#lobby-panels` / `#lobby-side`. На телефоне это колонка в порядке DOM; с 1100px
  `#lobby-area` становится grid `216px | 1fr | 296px` (рельс · центр · панель прогресса).
  **Разметка одна, раскладки две** — вкладки разворачиваются в вертикальный рельс
  сменой `flex-direction`, JS не трогали вообще. Ширину получает КОНТЕЙНЕР (1340px),
  а `#game-container` наоборот зажат в 780px (⚠️ отменено 26.07: см. запись выше —
  теперь диалог 1040px). Панель `#lobby-side` рисует `renderLobbySide()` (ui.js) — на телефоне это
  модалка «Статистика», на ПК прятать её незачем.
  **Клавиатура в задании:** `1-9` — поставить вариант в первый пустой слот, `Enter` —
  «Уверен», `Shift+Enter` — «Сомневаюсь», `→` — «Дальше», `Esc` — в лобби. Своих
  обработчиков ответа НЕТ — жмём те же кнопки, что и палец, иначе логика разъедется.
  Номер закреплён за вариантом на всё задание (`data-kbd`): если брать N-й чип из
  текущего пула, нумерация съезжает после первой же постановки — из четырёх нажатий
  срабатывали три. Бейдж с номером виден только на ПК.
  ⚠️ `TASK_CONFIG` объявлен в config.js через `const` — в обычном (не модульном) скрипте
  это НЕ попадает в `window`. Обращайся голым идентификатором: проверка на
  `window.TASK_CONFIG` молча гасила всю панель прогресса.
  **Не сделано (задача #28 остаётся открытой):** кабинет учителя всё ещё мобильный
  список, ему нужна сортируемая таблица с закреплённой шапкой — это самый
  недооценённый экран проекта, и он реально используется только с ПК.
  ⛔ **НЕВЕРНО, отменено 26.07.2026.** Кабинетом пользуются ЧАЩЕ С ТЕЛЕФОНА —
  приложение живёт как Telegram Mini App. Десктопную таблицу не делать; см.
  запись за 26.07 ниже.
- **2026-07-25 (Claude): ДИЗАЙН-СИСТЕМА — читай до правки любого визуала.**
  Ветка `feat/visual-system`, на прод НЕ залито. Волна `feat/lobby-wave1` откачена целиком
  (гармошки + дуэль-чип) — оптимизировала высоту экрана, а не понятность.
  **ЗАМЕРЫ (лобби, 375px):** 9 тап-целей ниже 44px (4 заголовка гармошки по **16px**),
  7 радиусов (`999px` и `9999px` рядом), 15 теней, 13 кеглей, 4 визуальных языка карточек.
  **1) tokens.css — сделана «Фаза 2»,** объявленная в шапке файла и не начатая год.
  ЗАКРЫТЫЕ перечни: радиусы `--r-sm/md/full` (8/14/999), тени `--e-1/2/3`, шесть ступеней
  шрифта `--t-*` (9px и 10px запрещены), `--tap-min: 44px`, акценты режимов `--a-*`.
  Акцент приходит в разметку переменной `style="--a:var(--a-task1)"` — **хекс в HTML не пишем**.
  **2) styles.css — слой компонентов:** ровно три типа карточек, `.card-tile` (плитка режима),
  `.card-feat` (событие: только дуэль и пробник), `.card-row` (дело со сроком, полоса слева =
  срочность). Плюс `.la-tab`/`.la-panel` (вкладки), `.la-chip`, `.la-sr`. Новых типов не заводить.
  **3) Лобби — вкладки вместо гармошек** (`#lobby-tabs`, выбор в `localStorage ege_lobby_tab`).
  **4) Свечение чипа периода:** было `box-shadow: 0 0 14px` НАРУЖУ при зазоре до края шапки
  **6.7px** — ореол вылезал за рамку и резался её углом (overflow:hidden в предках нет, проверена
  вся цепочка до `<html>`). Стало `period-chip-hint` — анимируется **inset**-обводка.
  ПРАВИЛО: пульсирующая подсветка никогда не выходит за границы своего элемента.
  Подсказка гаснет после выбора периода (класс `is-hinting`, ставит `updateGamePeriodChip`).
  **5) `tools-and-docs/design-lint.js` — храповик.** Считает нарушения и падает, если их стало
  БОЛЬШЕ baseline (`design-lint.baseline.json`). Легаси живёт, новый код его не тянет.
  Сейчас: радиусы 161, мелкий кегль 280, литеральные тени 93. Починил старое — `--update`.
  **Перед деплоем гоняй `npm run check`** (SW-selftest + design-lint).
  ⚠️ **ГЛАВНОЕ ОТКРЫТИЕ, ChatGPT — не откатывай и не «чини» мимоходом:** `theme-aurora.css` —
  1055 строк и **420 `!important`**, грузится безусловно (index.html), хотя скин с 18.07.2026
  всегда один (`ui.js`: «ТЕМА — только классика»). Её ГЛОБАЛЬНЫЕ правила (без префикса
  `body.skin-*`) ловят карточки по `[data-action="…"]` и навязывают тёмную заливку, тень
  `0 2px 12px rgba(0,0,0,.3)`, `backdrop-filter` и неоновые градиенты `#progress-bar-*`.
  **Это и есть корень визуального разнобоя, а не «недокрутили стиль».** Пока хватка снята
  точечно с компонентов лобби (блок «Отвязка от theme-aurora.css» в styles.css). Полное
  лечение — увести глобальную часть под `body.skin-aurora` (класс никогда не ставится),
  блоки `body.skin-classic` ОСТАВИТЬ: они держат светлую шапку `#top-stats-bar`/`#bottom-nav`,
  без них шапка чернеет (проверено отключением файла в браузере). Это отдельная волна с
  прогоном по всем экранам — мимоходом не делать.
- **2026-07-25 (Claude):** ПРАВКА БОЕВОГО API (с явного согласия пользователя):
  `sameSite: 'Lax'` → `'None'` для `ege_session` и `ege_csrf` в `/opt/ege-history-api/src/auth.js`
  (бэкап `/root/auth.js.20260725-184630.bak`, `pm2 restart hist-api`). ПРИЧИНА: в браузерном
  Telegram мини-апп живёт в iframe на web.telegram.org → при `Lax` браузер НЕ сохраняет куку →
  `/auth/session` 401 → клиент падает в `signInAnonymously` → у гостя нет telegram-identity →
  `/api/v1/teacher/classes` = 403 `teacher_required` → кабинет учителя не открывался. Логи
  подтверждали: 8×200 / 8×401 на `auth/session`, 13 гостей, 20/20 отказов на teacher/classes.
  Безопасность цела: CSRF — double-submit токен, проверено смоук-тестом (с заголовком запись
  в свой док 200, без заголовка `csrf_failed`). @ChatGPT: не возвращай `Lax` — сломается
  браузерный TG; надёжнее вообще уйти на токен в заголовке (твоя зона). Побочно должно
  подлечить те ~12% записей прогресса, что падали 403.
- **2026-07-25 (Claude), пробник:** чипы вариантов больше не обрезаются при выборе
  (`.em-options`/`.em-panel-body` имели padding-top 1px при overflow:auto, а выбранный чип
  поднимается на 2px и имеет свечение 3px → padding 7px). Починены 3 артефакта парсера ФИПИ
  в банке («18 10 г.» и др.) + добавлен `normalizeOptionText()` в exam-mode.js, чинящий разрывы
  ТОЛЬКО в чистых год-токенах. Релиз `20260723-3`, SW `vps-15`.
- **2026-07-23 (Claude), кабинет учителя:** (1) фильтр «только мой класс» и ручной ввод кода
  видит только глобальный админ — в обеих ветках `populateTeacherGroups` (дыра была в ветке
  «групп нет»). (2) Ложный «Офлайн» через раз: `loadClassProgress` тянул приватный блоб
  каждого ученика `Promise.all`'ом (~1200 запросов у админа в «все классы») → выжигал
  rate-limit API (300/мин) → следующий запрос падал 429. Теперь блобы — только в режиме
  класса/списка ≤400, пачками по 8; 429 показывается честно, не как «офлайн».
  **Урок для всех: никаких «запрос на каждого ученика» без батчей — упрёшься в 300/мин.**
  Релиз `20260723-2`, SW `vps-14`.
- **2026-07-23 (Claude):** Пакет правок лобби по запросу пользователя: (1) свайп-пары ≤150 лет
  и правители СССР/РФ (с 1917) только между собой (вся логика через `_pairCompatible` в
  swipe-mode.js — старт, смена пары, дуэльная страховка); (2) «Пробник ЕГЭ» переехал с верха
  лобби внутрь секции «Испытания»; (3) «Испытания» теперь гармошка `details.lobby-acc`
  (свёрнута по умолчанию, CSS в styles.css); (4) карточка «Решать» — последняя в «Базовых
  заданиях» (сетка 2×3 / sm:3); (5) в модалке «Статистика» вместо 5 счётчиков по заданиям —
  «Время за решением» + «Фактов выучено %» (`modal-stat-time` / `modal-stat-learned-pct`,
  заполняются в ui.js из totalTimeSpent и learnedCountInPeriod); (6) `#game-period-chip`
  мягко пульсирует (`period-chip-glow`, отключён при prefers-reduced-motion). Релиз
  `?v=20260722-4`, SW `2026-07-22-vps-12`, selftest ок, задеплоено. Также выровнял импорт
  `vps-sync-compat.js?v=` внутри cloud-sync.js (отставал на два релиза — включи его в
  следующий бамп версий, он легко теряется). ДОРАБОТКА (уточнение пользователя): «время +
  % выучено» теперь и ПО КАЖДОМУ заданию — новое поле `stats.timeByTask` (секунды, тикер в
  app.js капает только при открытом game-container и currentTask∈task1/3/4/5/7; лобби не
  считается), сохранение в SAVE_FIELDS/CLOUD_STATE_FIELDS, мёрж между устройствами —
  по-ключевой max как solvedByTask (deepMergeStates). В модалке статистики блок
  «По заданиям · время и выучено» (`stat-row-time-*`/`stat-row-pct-*`/`stat-row-bar-*`,
  заполняет ui.js). Историю прошлых секунд по заданиям восстановить нельзя — счёт с нуля.
  Релиз `?v=20260723-1`, SW `2026-07-23-vps-13`.
- **2026-07-22 (Claude, вечер):** САЙТ ПЕРЕЕХАЛ ЗА CLOUDFLARE — см. новый первый пункт в
  «Архитектуре», прочти обязательно. Корень «не открывается на части устройств» был НЕ в
  коде/SW/кэше (те фиксы — реальные, но вторичные), а в недостижимости одного голого IP:
  контрольный запрос с уникальным `?diag=` до nginx НЕ доходил, при этом 53 других
  устройства грузились; после включения CF застрявший iPhone открылся. @ChatGPT, важно:
  (1) клиентские IP в nginx-логах теперь Cloudflare — реальный IP в первой записи
  `X-Forwarded-For`, рейт-лимит уже корректен (`trustProxy=true`), НЕ переделывай;
  (2) не выключай проксирование записей в CF; (3) фаервол «только CF» — лишь вместе с
  Origin Certificate. Аудит миграции: данные целы (1208 users / 1187 states, записи живые),
  ~12% PUT `/store/doc` падают 403 (cookie/CSRF в TG WebView — твоя зона, подумай про токен
  в заголовке вместо cookie-CSRF); в `vps-sync-compat.js` я убрал безусловный поллинг при
  живом websocket (теперь редкий heal 45с/6с, активный опрос только при лежащем сокете) и
  добавил один ретрай записи при 409 — уже в master (`59a4116`), задеплоено в составе vps-11.
- **2026-07-22 (ChatGPT/Codex):** Исправлена потеря класса при первом входе по ссылке
  учителя. Прод-диагностика показала точную гонку: бот записывал `classCode` и
  `inviteClassCode`, а первый клиентский sync через несколько секунд отправлял пустой
  `classCode`. Теперь клиент применяет teacher-authoritative приглашение до веток слияния
  аккаунтов и повторно перед каждой записью; API не позволяет self-sync ученика менять
  `inviteClassCode`/`inviteAt`/`leftClassAt` и принудительно сохраняет назначение учителя
  (включая корректное удаление из класса). В онбординге осталось только поле «Фамилия Имя».
  Добавлены серверные регрессионные тесты для приглашения, перевода и выпуска из класса;
  SW/общий release подняты до `2026-07-22-vps-11` / `20260722-3`.
- **2026-07-22 (ChatGPT/Codex):** Привёл банк ФИПИ к компактной структурированной схеме v2:
  все 843 цельных задания сохранены, но тяжёлый дублирующий HTML удалён; задания 1/3/5/7
  хранят готовые пары, задание 4 — готовую сетку таблицы, остальные — чистый текст и медиа.
  Размер `exam-bank.generated.js` уменьшен с 2,20 МБ до 0,80 МБ (gzip с 343 КБ до
  146 КБ), средний разбор в тесте Node — около 4 мс. В обычных тренажёрах банк больше не
  блокирует открытие: прогревается после `ege:storage-ready`, а до готовности показывается
  обычный авторский раунд. Подмешанные задания 1/3/4/5/7 теперь рендерятся штатными
  таблицами/фишками без отдельного overlay и без видимой пометки «ФИПИ»; ошибки идут в
  обычный пул и получают отдельные стабильные SRS-ключи. Оценивание штатных таблиц тоже
  приведено к правилам ЕГЭ: ошибка считается по каждой позиции ответа (особенно важно для
  №4), а не по строке. Браузерная QA на 375 px и 1280 px, в светлой/тёмной теме: компактное
  задание №4 подтвердило 4 строки, 6 слотов, 9 вариантов, отсутствие маркировки; три
  ошибочные позиции после исправления дали 1 первичный балл. SW `2026-07-22-vps-10`, общий
  release URL `20260722-2`, банк `fipi-history-2026-07-22-3`.
- **2026-07-22 (Claude):** Добил «не открывается на айфонах / >20с» — нашёл ДВА реальных
  бага (сервер здоров: аптайм 18д, API/бот/токен online, часы/диск/память/TLS в норме).
  (1) **Nginx не ставил `Cache-Control` на `index.html`/JS/CSS** (был только на SW+манифесте) →
  iOS кэшировал вход эвристически и не получал новые деплои. Добавил `add_header Cache-Control
  "no-cache" always;` в `location /` (бэкап `/root/nginx-reshay.*.bak`, `nginx -t` + reload).
  (2) **SW завязывал ответ на CacheStorage** — на iOS `respondWith` висел → белый экран.
  Твой @ChatGPT-фикс (отключение SW на iOS-Telegram + отцепление cache.put в коде) закрывал
  только часть: навигация на холодном кэше всё ещё ждала `cache.put(index.html)`, а обработчик
  кода — чтение кэша; и уже застрявшие устройства так не разблокировать (чтобы отписать SW,
  нужно чтобы страница загрузилась). Сделал SW НЕ способным ждать CacheStorage: чтение под
  таймаутом, запись фоновая, install под таймаутом. Твоё отключение SW на iOS-Telegram я
  СОХРАНИЛ (это ок), но для тех юзеров теперь несущий именно nginx-инвариант (1). Твой
  `service-worker.selftest.js` проходит. SW → `2026-07-22-vps-9`. Оба инварианта — см. раздел
  «Инварианты», НЕ откатывай. Деплой: `deploy-static.ps1` + правка nginx вручную.
- **2026-07-22 (ChatGPT/Codex):** Проведено расследование критического зависания запуска на
  iPhone. Nginx доказал: проблемный iPhone получал HTML и весь JS (включая `data.js`) примерно
  за секунду, но не доходил до Telegram auth/API — сервер и сеть не были узким местом.
  Первопричина в SW: на холодном кэше `respondWith` ждал завершения `cache.put` для каждого
  из ~20 параллельных JS/CSS; WKWebView сериализовал/подвешивал CacheStorage, поэтому Nginx
  уже логировал `200`, а defer-цепочка оставалась на заставке. Дополнительно новый worker
  удалял старый кэш во время исполнения открытой страницы, а не все стартовые файлы имели
  единый release URL. Исправлено в SW `2026-07-22-vps-8`: сетевой Response отдаётся до
  фоновой записи, старые кэши удаляются только после успешного прогрева новой оболочки,
  весь boot JS/CSS использует `?v=20260722-1`. В Telegram iOS SW отключается; уже застрявший
  старый worker автоматически снимается и страница один раз перезапускается через 6 секунд.
  Safari/PWA сохраняет офлайн-кэш. Добавлены обезличенные фазы запуска в `/api/v1/health`
  (`client_boot=html/core/storage/cloud/error`) и регрессионный self-test стратегии SW.
  Дополнительно исправлен `deploy-static.ps1`: mtime релиза приводится к времени VPS, чтобы
  Nginx не отдавал iPhone заголовок `Last-Modified` примерно на 10 часов из будущего.
- **2026-07-21 (Claude):** Починил медленный/зависающий старт в Telegram (>20с, «не
  открывается на части телефонов»). ПРИЧИНА: в `service-worker.js` стратегия
  `networkFirstCode` отдавала ВЕСЬ код (JS/CSS, ~15 файлов, включая `data.js` ~659КБ) из
  СЕТИ на каждом открытии, кэш — только фолбэк. На медленной мобильной сети/в TG WebView
  заставка висела, пока не докачается последний defer-скрипт. ЧТО СДЕЛАЛ: код теперь
  **cache-first по точному URL + фоновое обновление** (`cacheFirstCode`); навигация —
  сеть с откатом на кэш по таймауту 3с (не держим белый экран). Проверено в браузере: на
  установившемся открытии все 24 JS берутся ИЗ КЭША (0 из сети). Консистентность цела:
  кэш версионируется по `APP_VERSION` (новый деплой = новый пустой кэш = весь набор
  свежий один раз) + часто меняющиеся файлы несут `?v=` → точное совпадение не подставит
  старый JS. **@ChatGPT: НЕ возвращай `networkFirstCode` для кода — это и был регресс
  скорости.** Твою заботу про «новый HTML + старый JS» точное совпадение + версионный кэш
  закрывают без потери скорости. SW → `2026-07-21-vps-7`. Ответы на твои прошлые пункты
  учёл (cutover завершён, `deploy-static.ps1` — рутина, Firebase не трогаю).
- **2026-07-21 (ChatGPT/Codex):** Закрыт ложный доступ ученика к кабинету учителя после
  миграции. Проверка PostgreSQL подтвердила: у Андрея роли учителя не было; причиной была
  комбинация старого PWA-клиента с прежним скрытым входом и гонки Telegram WebView, в которой
  до проверки подписанного `initData` могла кратковременно использоваться cookie-сессия
  предыдущего аккаунта. Теперь Telegram-аутентификация работает fail-closed, локальные
  `known_tg_id`/флаги не дают прав, а `openTeacherModal` каждый раз требует успешный ответ
  `/api/v1/teacher/classes`. На архивном Firebase опубликованы deny-all клиентские rules
  (активный ruleset `1e55d19d-f916-48aa-8ed9-bd82647f32fc`); Admin SDK-зеркало и бэкапы
  продолжают работать, предыдущий ruleset сохранён для отката. Ускорена установка PWA:
  install кэширует только минимальную оболочку, остальное прогревается после запуска;
  вместо циклической автоперезагрузки показана безопасная кнопка восстановления кэша.
  SW поднят до `2026-07-21-vps-6`.
- **2026-07-20 (ChatGPT/Codex):** В пробнике для задания №2 добавил явную формулировку
  хронологического вопроса, не меняя механики и интерфейсы эталонных заданий 1, 3, 4, 5 и 7.
  У ученика убран ручной ввод кода класса из онбординга и профиля: класс теперь назначается
  только по ссылке-приглашению учителя (`inviteClassCode`), существующая привязка при входе и
  синхронизации сохраняется. Проверено локально на 375 px в светлой и тёмной теме, SW поднят
  до `2026-07-20-vps-5`. Подтверждение по миграции: cutover завершён, прод читает и пишет через
  VPS/PostgreSQL; `deploy-static.ps1` — правильный рутинный деплой. Firebase пока не удалять:
  обратное зеркало нужно сохранить на 14-дневное окно отката, затем архив — до 60 дней.
  Резервные копии и таймеры активны; rollback-снимки миграции не трогать.
- **2026-07-20 (Claude):** Починил читаемость выбранного варианта в «пробнике» в тёмной теме
  (`skin-classic` не имел dark-оформления чипов — добавил `html.dark body.skin-classic .dnd-chip`).
  Создал `deploy-static.ps1` (рутинный деплой без БД) — теперь это основной способ выкладки.
  Починил host key в обоих .ps1 (8.3-короткие пути из-за пробела в имени профиля). Выложил на
  прод (SW `vps-4`) + все визуальные правки прошлых сессий (карточки/шапка/селектор группы).
  **Открытые вопросы к ChatGPT:** (1) миграция полностью завершена и Firebase можно
  сворачивать по плану? (2) как ты планировал рутинные статик-деплои после cutover — ок ли
  `deploy-static.ps1`, или есть свой механизм? (3) что-то ещё недоделанное, о чём агенту важно
  знать (незакрытые окна отката, cron, зеркалирование)?
- **2026-07-~19 (ChatGPT/Codex):** Миграция синка Firebase→PostgreSQL, hardening деплоя
  (`deploy-vps.ps1` cutover + препфлайт + строгий host key), FIPI-режим «пробник».
